# Washora Admin

Mobile-first React PWA for Washora laundry order tracking, expenses, pricing, and admin dashboards.

Project plan and technical notes live in:

```text
docs/WASHORA_PROJECT_PLAN.md
```

Firebase setup steps live in:

```text
docs/FIREBASE_SETUP.md
```

Migration steps live in:

```text
docs/MIGRATION_GUIDE.md
```

## Local Commands

```powershell
npm install
npm run dev
npm run build
```

Copy `.env.example` to `.env` and add Firebase web app values before using the real app storage.
